---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data
hierarchy:
  - name: "Operating System Family "
    path: "os/family/%{facts.os.family}.yaml"

  - name: "Defaults"
    path: "common.yaml"
